# Example Package

This package is solely for testing.

It exposes a `value` with a constant `String`, _"This is the value"_.

This package should not be used for anything, it's only for testing.

This is extra content to make the README long enough to publish.

This is extra content to make the README long enough to publish.

This is extra content to make the README long enough to publish.

This is extra content to make the README long enough to publish.

This is extra content to make the README long enough to publish.

This is extra content to make the README long enough to publish.

This is extra content to make the README long enough to publish.

This is extra content to make the README long enough to publish.

This is extra content to make the README long enough to publish.
